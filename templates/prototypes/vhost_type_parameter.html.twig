{# templates/vhost_type/_parameter_prototype.html.twig #}

<div class="card bg-light mb-3" data-collection-item>
    <div class="card-body">
        <h5 class="card-title d-flex justify-content-between align-items-center">
            Parámetro
            <button type="button" class="btn btn-danger btn-sm" data-action="form-collection#removeItem">
                Eliminar
            </button>
        </h5>
        <div class="row">
            <div class="col-md-6">
                {{ form_row(parameter.name) }}
            </div>
            <div class="col-md-6">
                {{ form_row(parameter.description) }}
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                {{ form_row(parameter.dataType) }}
            </div>
            <div class="col-md-6">
                {{ form_row(parameter.defaultValue) }}
            </div>
        </div>
    </div>
</div>
